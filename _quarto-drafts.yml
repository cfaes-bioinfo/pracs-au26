# Local-only profile: render/preview with draft pages visible and linked.
# Usage: quarto preview --profile drafts
# The default profile keeps `draft-mode: gone` (see _quarto.yml), so the
# published site is unaffected.

website:
  draft-mode: visible
